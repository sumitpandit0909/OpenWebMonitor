import React from 'react';

const Dashboard = () => {
  return (
    <div className="dashboard">
      <h1>Your Dashboard</h1>
      {/* Add logic to display tracked pages */}
      <button>Add New Page</button>
    </div>
  );
};
export default Dashboard;